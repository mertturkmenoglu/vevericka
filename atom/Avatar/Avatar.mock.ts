import type { AvatarAtomicProps } from './Avatar.type';

const mock: AvatarAtomicProps & { src: string } = {
  src: 'https://github.com/mertturkmenoglu.png',
  label: 'Profile picture',
  size: 'default',
  appearance: 'circle',
};

export default mock;
