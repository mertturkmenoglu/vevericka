import { AppBar } from '../../components';

function Feed(): JSX.Element {
  return (
    <main>
      <AppBar className="mx-auto mt-4" />
      <div>Feed</div>
    </main>
  );
}

export default Feed;
